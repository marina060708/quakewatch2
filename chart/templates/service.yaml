apiVersion: v1
kind: Service
metadata:
  name: {{ include "quakewatch2.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
  selector:
    app: {{ include "quakewatch2.name" . }}
